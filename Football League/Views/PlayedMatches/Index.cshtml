@model IEnumerable<Football_League.Models.PlayedMatch>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.sideA)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.sideB)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.matchResult)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.winner)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.sideA)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.sideB)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.matchResult)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.winner)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.matchId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.matchId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.matchId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
